export interface TokenPrices {
  SOL: number;
  USDC: number;
  USDT: number;
  ETH: number;
}

export interface TokenBalance {
  tokenSymbol: string;
  tokenBalance: number;
  tokenLogoURI: string;
}
